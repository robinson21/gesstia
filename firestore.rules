rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Verificar autenticación base
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
